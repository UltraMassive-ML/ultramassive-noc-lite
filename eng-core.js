// @umml/noc-lite :: engine/router
// STATUS: extraction target (Phase 1) — NOT YET WIRED.
// The working implementation currently lives inline in /index.html (index.html:874).
// Owns after extraction: executeNOCRouting() tier heuristic, floor escalation, RouteDecision shape
// See docs/ARCHITECTURE.md for the full extraction map and module contract.
export const EXTRACTION_PENDING = true;
