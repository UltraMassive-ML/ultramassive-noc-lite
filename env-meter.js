// @umml/noc-lite :: engine/possibilistic
// STATUS: extraction target (Phase 1) — NOT YET WIRED.
// The working implementation currently lives inline in /index.html (index.html:874, 909).
// Owns after extraction: possibility/necessity (PI, N) band computation + threshold evaluation
// See docs/ARCHITECTURE.md for the full extraction map and module contract.
export const EXTRACTION_PENDING = true;
