// @umml/noc-lite :: ui/auth-gate
// STATUS: extraction target (Phase 1) — NOT YET WIRED.
// The working implementation currently lives inline in /index.html (index.html:801, 1408).
// Owns after extraction: sign-in modal, session cookie, mountNOC() gate + tier locking
// See docs/ARCHITECTURE.md for the full extraction map and module contract.
export const EXTRACTION_PENDING = true;
